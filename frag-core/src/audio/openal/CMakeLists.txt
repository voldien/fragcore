
# GCC compiler flag options.
IF ((CMAKE_COMPILER_IS_GNUCC OR CMAKE_COMPILER_IS_GNUCXX) AND NOT CMAKE_COMPILER_IS_MINGW)

ENDIF ()

FIND_PACKAGE(OpenAL)

# OpenGL dependies headers.
INCLUDE_DIRECTORIES(${ALUT_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${OPENAL_INCLUDE_DIR})

# Source files.
FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

ADD_LIBRARY(fragcore-aal ${SOURCE_FILES} )
TARGET_LINK_LIBRARIES(fragcore-aal  ${OPENAL_LIBRARY} ${ALUT_LIBRARY} fragcore)
ADD_DEPENDENCIES(fragcore-aal fragcore)

IF(BUILD_WITH_INSTALL)
	INSTALL(TARGETS fragcore-aal DESTINATION lib COMPONENT ${LIBFRAGCORE_COMPONENT})
ENDIF()