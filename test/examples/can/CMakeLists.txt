FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB SOURCE_COMMON ${CMAKE_CURRENT_SOURCE_DIR}/../common/*.cpp)
FILE(GLOB SOURCE_COMMON_HEADER ${CMAKE_CURRENT_SOURCE_DIR}/../common/*.h)

# Executable OpenGL hpm benchmark.
ADD_EXECUTABLE(can-sample ${SOURCE_FILES} ${SOURCE_COMMON} ${SOURCE_COMMON_HEADER})
TARGET_LINK_LIBRARIES(can-sample PRIVATE fragcore fragcore-ionet-can)
TARGET_INCLUDE_DIRECTORIES(can-sample  PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../common/ )
ADD_DEPENDENCIES(can-sample  fragcore fragcore-ionet-can)
